# whimsey

Static app ready for Vercel deployment.

## Deploy on Vercel

- Root route serves `/index.html`.
- Static assets are served directly from repository root.
- `vercel.json` includes the root rewrite for `/`.
